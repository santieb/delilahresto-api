const ordersList = [];

const states = ["cancelled", "new", "confirmed", "preparing", "sending", "delivered"]

module.exports = {
    ordersList,     
    states                                                                                              
};